//! Generated gRPC/protobuf types for `proto/intelligence.proto` (built by
//! `build.rs` via `tonic-prost-build`). Re-exported as one module so the rest
//! of the crate (and downstream callers like `server`) has one import path.

tonic::include_proto!("intelligence.v1");
