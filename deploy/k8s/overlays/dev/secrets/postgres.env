# DEV ONLY — local-cluster credentials, same posture as .env.example.
POSTGRES_DB=mev
POSTGRES_USER=mev
POSTGRES_PASSWORD=changeme_postgres_password
DATABASE_URL=postgres://mev:changeme_postgres_password@postgres:5432/mev?sslmode=disable
